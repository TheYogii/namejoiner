---
import ToolCard from './ToolCard.astro';
import { tools } from '../data/tools';
---
<ul id="tools" class="grid scroll-mt-24 gap-4 sm:grid-cols-2 lg:grid-cols-3">
  {tools.map((tool) => <ToolCard tool={tool} />)}
</ul>
